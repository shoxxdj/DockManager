//Configuration file
//Devices is an array with all the docker servers you want to manage
 devices = ['localhost'];
//Port is the port where this app has to connect to contact the docker service on the device's list
 port = "31337";

