
<% var panel_colors=["default","default","default","default"]; %>
<% 
	var passages=0;
	for (var i=0;i<res.length;i++)
	{
		ip=res[i];
		i++;
		re=res[i];
%>

	<% if(passages%3==0)
		{
	%>
	<div class="row">
	<%
		}
	%>
		<div class="col-md-3 table-responsive">
		<%= ip %>
			<table class="table ">
<%
			for(var j=0;j<re.length;j++)
			{	
%>
				<tr>
					<td>
						<span onclick="actionImage('<%= ip %>','<%= (re[j]["Id"]).substring(0,12) %>','<%= re[j]["RepoTags"] %>')"class="glyphicon glyphicon-play"></span>
						
					</td>
					<td>
						<%= re[j]["RepoTags"] %>
					</td>
				</tr>
			
<%
			}
%>
			</table>
		</div>
	<% if((passages+1)%3==0)
		{
	%>
	</div>
	<%
		}
	%>
<%
	}
%>
<%
	if(passages%6==1)
		{
	%>
			</div>
	<%
		}

%>

